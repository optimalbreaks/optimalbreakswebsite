# Documentación del repositorio — índice y auditoría

Mapa de los **Markdown** del proyecto, para saber qué leer primero y qué mantener al día.

---

## Índice de archivos `.md`

| Ubicación | Propósito |
|-----------|-----------|
| **[`README.md`](../README.md)** (raíz) | Documentación técnica **canónica** (inglés): stack, env, auth, Storage, DJ deck, **buscador global ⌘K** (`CommandPalette` + `/api/search`: nueve orígenes, dedupe por edición reciente, deep-link + autoplay), **sistema de audio global** (`DeckAudioProvider`, `claimAudio`, cinco fuentes mutuamente exclusivas, barra flotante compartida), **línea temporal de la home** (`home.section_history` en diccionarios: orden **editorial manual**, sin sort por año), **tira de eventos** (hasta 4 próximos / fallback), migraciones (tabla parcial), scripts `npm`, estructura de carpetas, **listado de eventos** (`EventsExplorer`: semáforo, hover, `group/link`, **vista calendario** rojo/amarillo por día), **CTA MonsterTicket** en ficha de evento, **página mixes** (filtros, embeds perezosos YouTube/SoundCloud, `MixesExplorer`), **Beatport Top 10** en fichas (`046`, `db:beatport:top`, vs chart `db:chart`). |
| **[`README.es.md`](../README.es.md)** | Resumen en **español**: flujo de artistas, auth, GA4, vistas de listado (incl. **eventos** y **mixes**: filtros y embeds), **buscador global** (palette ⌘K, dedupe, deep-link), **sistema de audio global** (exclusión mutua, barra flotante), **Beatport Top 10** en artistas/sellos (`db:beatport:top`), **timeline + tira de eventos en la home**; enlaza al README en inglés para el detalle largo. |
| **[`docs/AI_PROMPTS_AND_AGENTS.md`](./AI_PROMPTS_AND_AGENTS.md)** | Índice de **prompts** (`scripts/prompts/*.txt`), variables OpenAI/SerpAPI, modelo por defecto **por flujo**, rutas API relacionadas. Bilingüe (ES/EN en un solo archivo). |
| **[`docs/ARTIST_AI_AGENT.md`](./ARTIST_AI_AGENT.md)** | Manual del **agente de artistas** (bios): CLI, batch, API admin, UPSERT por REST. Incluye además **fotos de artista** (`db:artist:photo`, `--repair`, retratos en `public/images/artists`, sync del mapa). |
| **[`docs/IMAGES_AND_WEBP.md`](./IMAGES_AND_WEBP.md)** | `displayImageUrl`, **`displayArtistImageUrl`** (artistas), `public/images/` vs Supabase Storage, reglas WebP, SEO/OG. |
| **[`docs/USER_ENGAGEMENT.md`](./USER_ENGAGEMENT.md)** | Favoritos, «seen live», asistencia, valoraciones, **Mis Tracks** (`saved_chart_tracks`, **053** + **054**), lista **`/u/<id>/tracks`**, admin Tracks; **Top de la Comunidad** all-time (`GET /api/public/charts/community-monthly`, `CommunityMonthlyTop` — slug histórico), **Almas Gemelas** (`GET /api/breakbeat/soulmates`, migración **056** `profiles.is_tracks_public`), mapa `/mi-cuenta/<slug>`. Además **enlaces compartibles por canción** (`TrackShareButton`, `src/lib/share-track.ts`) y **fallback de autoplay** (`previewBlocked`, `PreviewAutoplayOverlay` en `DeckAudioProvider`, carátula con `next/image` por hotlink Beatport). |
| **[`mailing/supabase/README.md`](../mailing/supabase/README.md)** | Plantillas HTML de **correo** para pegar en Supabase Auth (`RedirectTo` + `TokenHash`). |
| **[`public/images/README.md`](../public/images/README.md)** | Convención **operativa** para assets estáticos (WebP, `npm run images:to-webp`). Alineado con la regla de Cursor `imagenes-public-webp`. |

Este archivo (**`docs/README.md`**) es solo índice y auditoría; no duplica procedimientos largos.

---

## Auditoría: ¿sobra? ¿falta?

### Qué no sobra (complementario, no redundante)

- **Dos README de raíz** — `README.md` + `README.es.md` cumplen roles distintos: el inglés es la referencia completa; el español evita repetir páginas de texto y enlaza al inglés.
- **`AI_PROMPTS_AND_AGENTS.md` vs `ARTIST_AI_AGENT.md`** — El primero es **índice transversal** (todos los agentes y `.txt`); el segundo es **profundidad solo en artistas**. La repetición mínima (comandos `npm`, env) es aceptable para quien solo abre un archivo.
- **`IMAGES_AND_WEBP.md` vs `public/images/README.md`** — El de `docs/` explica **código y URLs en BD**; el de `public/images/` es **flujo de archivos en disco**. Conviven bien.

### Duplicación a vigilar (no urgente)

- Comandos **`db:artist`**, **`db:artist:agent`**, **`db:artist:photo`** / **`db:artist:photo:repair`**, **`db:artist:sync-public-portraits`** y variables Supabase aparecen en README, README.es y `ARTIST_AI_AGENT.md`. Si cambia un comando, conviene actualizar **los tres** o al menos README + README.es y dejar el agente como detalle.

### Qué falta o es opcional

- **Tabla de migraciones en `README.md`** — Solo lista hasta **`011_*`** como ejemplo histórico; las migraciones **`012`–`039+`** (charts, mixes, OG, escenas, engagement, lotes) están solo en `supabase/migrations/`. El README principal indica que la tabla es **parcial**; no hace falta duplicar 30+ filas salvo que quieras un changelog de esquema.
- **`CHANGELOG.md`** — No existe; útil si publicas versiones con notas para humanos (opcional).
- **`CONTRIBUTING.md`** — Solo si aceptáis PR externos con convenciones explícitas.
- **Reglas de Cursor** (`.cursor/rules/`) — No están en esta lista: viven aparte y orientan a la IA en el editor.

### Resumen

**No hay que borrar** ningún `.md` actual: el conjunto es razonable para un repo con contenido editorial + Supabase + agentes. El mantenimiento costoso es solo **sincronizar** README/README.es cuando cambien comandos o env; el índice de prompts cuando añadas un `.txt` nuevo.

---

## Cambios recientes documentados en README

- **Top de la Comunidad + Almas Gemelas + migración 056:** **`056_community_top_and_soulmates.sql`** añade **`profiles.is_tracks_public`** (default `TRUE`) e **`idx_sct_created`** en **`saved_chart_tracks.created_at`**. **`GET /api/public/charts/community-monthly`** (slug histórico; el bloque dejó de ser mensual en abril de 2026 y ahora devuelve un ranking **all-time**) alimenta el bloque público en **`/[lang]/charts`** (`CommunityMonthlyTop.tsx`, después de *Retro Vinyl Picks*); sin selector de mes; orden por usuarios únicos → total saves → save más reciente → alfabético. **`GET /api/breakbeat/soulmates`** (cookie auth) calcula afinidad Jaccard + recomendaciones; página **`/mi-cuenta/almas-gemelas`**; toggle de privacidad en **`/mi-cuenta/perfil`**. Detalle en **`docs/USER_ENGAGEMENT.md`** y **`README.md`** (*User engagement*, tabla de rutas, tabla SQL migrations **056**).
- **Fechas de release con día (YYYY-MM-DD):** migración **`057_chart_featured_tracks_release_date.sql`** añade `chart_featured_tracks.release_date DATE` (complementa `release_year`); `chart_tracks.release_date` y los JSONB **`artists.beatport_top_tracks`** / **`labels.beatport_top_tracks`** llevan también el campo. Helpers nuevos en **`src/lib/share-track.ts`**: `formatTrackReleaseDisplay`, `effectiveReleaseYear`, `releaseSortTimestampMs`, `isBeatportArtworkUrl`. Scrapers ampliados (Beatport `__NEXT_DATA__.publish_date` y Bandcamp `data-tralbum.album_release_date`) en `chart-featured-upsert.mjs` (flag `--enrich-release-dates` + `--write-json`), `chart-40-breaks.mjs`, `beatport-top-tracks.mjs` y `backfill-new-releases-from-40breaks.mjs`. **`scripts/saved-tracks-backfill.mjs`** rellena `release_date` en `saved_chart_tracks.snapshot` de forma **aditiva** (`mergeSnapshotAdditive`), con flag **`--scrape-beatport`** para los `beatport_top` huérfanos. CSS: las filas usan `break-words` + `whitespace-nowrap` en la fecha para que `2026-04-27` nunca se parta en dos líneas. Documentado en **`README.md`** (sección *Track release dates*) y **`docs/USER_ENGAGEMENT.md`** (sección *Release-date display in saved snapshots*).
- **Veto editorial — DistroKid (Beatport label id `66449`):** distribuidor masivo, no es un sello curado de breakbeat. Se mantiene la cadena `"DistroKid"` en los JSON de picks (Beatport lo reporta como label de muchos auto-publicados) pero **no** se crea fila en `labels` ni `/labels/distrokid`. Lista de vetos en **`README.md`** (sección *Editorial vetoes*); ampliarla **solo** con confirmación explícita del usuario.
- **Mis Tracks (`/[lang]/mi-cuenta/tracks`):** migración **`053_saved_chart_tracks.sql`** (tabla polimórfica `chart | featured | vinyl` con `UNIQUE (user, source, id)` y RLS) + migración **`054_saved_chart_tracks_beatport_top.sql`** (añade `beatport_top` como 4ª fuente + columnas `canonical_url` y `snapshot` + backfill de URLs), hook **`useSavedChartTracks`** con **store compartido a nivel módulo** (sincroniza todos los botones `SaveTrackButton` de la página, **incluido el que ahora vive dentro del `MiniPreviewBar` global**), **agrupación canónica cruzada** (URL de Beatport/Bandcamp normalizada para chart/featured; **ID de vídeo de YouTube** `yt:<id>` para vinyl — no `discogs_url`, ver justificación en `USER_ENGAGEMENT.md`) y ahora también **matching por URL (`isSavedByUrl` / `toggleByUrl`)** para integrar el Top 10 de Beatport en fichas de artista/sello (una canción guardada desde un chart sale ya en verde en el Top 10 y viceversa). Página con orden / filtros multi-fuente de reproducción (Beatport, Bandcamp, YouTube) / Play All + Shuffle / barra seekable. **Botón "+/✓" sobre la pista que suena**: cada `PreviewTrack` lleva un campo `save: PreviewSaveData` (modo `ref` o `url`) populado por `ChartView`, `TracksSection`, `BeatportTopTracks` y `CommunityMonthlyTop`, y el `MiniPreviewBar` lo renderiza como `SaveTrackButton size=sm` a la derecha del contador. **Lista pública compartible** en `/[lang]/u/<userId>/tracks` (`/api/public/user-tracks` + service-role para lectura). **Dashboard admin Tracks** en `/[lang]/administrator/tracks` (`/api/admin/tracks`). Refactor adicional: el antiguo monolito `/dashboard` con pestañas se parte en **resumen + 7 páginas** bajo `/[lang]/mi-cuenta/<slug>` con `UserSectionShell` compartido y redirección desde `?tab=xxx`. Documentado en **`docs/USER_ENGAGEMENT.md`** (detalle), **`README.md`** y **`README.es.md`** (secciones *User engagement* / *My Breaks*).
- **Beatport Top 10 (fichas artista/sello):** migración **`046_beatport_top_tracks.sql`**, script **`scripts/beatport-top-tracks.mjs`**, npm **`db:beatport:top`**, acordeón en hero (`BeatportTopTracks`), audio por **`/api/audio-proxy`**. Diferencia explícita respecto al chart semanal **`db:chart`**. Resumen ES: **`README.es.md`**; detalle EN: **`README.md`** (sección *Beatport: weekly chart vs Top 10 on profiles*).
- **Sistema de audio global:** sección en **`README.md`** (*Global audio system*) y **`README.es.md`** (*Sistema de audio global*): tabla de fuentes (`deck`, `mix`, `preview` con sus aliases retrocompatibles), exclusión mutua (`claimAudio` → `ob-audio-claim`), barra flotante inferior `MiniPreviewBar` compartida por charts / Top 10 / Mis Tracks / Top de la Comunidad, **botón guardar `+/✓` sobre la pista actual** (mismo `SaveTrackButton` que en cada fila), `mediaSession`, **safe-area móvil**: el `paddingBottom` del player añade 10 px sobre `env(safe-area-inset-bottom)` y `BackToTop` sube el mismo offset para no rozar la home-bar de iPhone. **`MediaMetadata` no incluye año de lanzamiento como campo aparte:** en lock screen/iOS concatenar año al título o artista empeora el truncado; se documenta dejar strings limpios y mostrar año/fecha solo en UI in-app. Archivos clave en la tabla *Key files* del README.
- **Mixes / `MixesExplorer`:** filtros (año, plataforma, búsqueda título+artista); catálogo completo en DOM con visibilidad vía clase Tailwind `hidden` para no recargar embeds al limpiar filtros; iframes montados con `IntersectionObserver` + `loading="lazy"`. Detalle en **`README.md`** y **`README.es.md`** (sección *Directory listing views* / *Vistas de listado*).
- **Fotos de artista:** `db:artist:photo`, `db:artist:photo:repair`, retratos en `public/images/artists` + `data/artist-public-portrait-map.json`, `db:artist:sync-public-portraits`, prioridad en UI (`displayArtistImageUrl`) y fallback punk — descrito en **`ARTIST_AI_AGENT.md`**, **`IMAGES_AND_WEBP.md`**, **`README.md` / `README.es.md`** (tabla npm y sección retratos), **`public/images/README.md`**.
- **Audio e idioma:** `DeckAudioProvider` con **`key={lang}`** en `src/app/[lang]/layout.tsx` — al cambiar ES/EN se reinicia la sesión de audio (deck de la home, barra inferior, modo mix).
- **Navbar móvil:** `html`/`body` con `overflow-x` para ancho; **header** sin `overflow-x` para no recortar menús desplegables (cuenta, hamburguesa).
- **Timeline de la home (`section_history`):** el orden de `items` en `src/dictionaries/*.json` es **manual / narrativo** (no ordenación por fechas en código). Ver sección dedicada en **`README.md`** y **`README.es.md`**.
- **Tira de eventos en la home:** hasta **4** filas con `date_start` ≥ hoy (orden ASC); si no hay datos, fallback por fecha DESC y luego placeholders estáticos — `src/app/[lang]/page.tsx`. Resumido en **`README.md`** / **`README.es.md`** (misma sección que la timeline).
- **Listado `/events` (`EventsExplorer`):** pie **semáforo** (pasado `var(--red)` + texto blanco; próximo `var(--yellow)` + `var(--ink)`), hover con `color-mix`, franja detrás del cartel alineada al estado, tarjeta **`group/link`**, zoom del cartel vía **`CardThumbnail`** con **`groupHoverGroup="link"`**, pies con altura homogénea en la rejilla; **vista calendario anual** con el mismo criterio por día (`calendar_legend_past` / `calendar_legend_upcoming`); **clic en día** → modal resumen + enlace a ficha (`calendar_modal_*`). Detalle en **`README.md`** (*Directory listing views*).
- **Ficha de evento (`/[lang]/events/[slug]`):** CTA ancha en el hero si hay URL de compra, el evento no está pasado por calendario y (`event_type === 'upcoming'` **o** enlace **MonsterTicket** en `tickets_url` / `website`); prioridad de URL y copy “Compra de entradas” / “Buy tickets” para MonsterTicket — ver **`README.md`** (*Directory listing views*).
- **OG por sección (mixes / charts):** imágenes estáticas **`public/images/opengraph/sections/mixes-screenshot.png`** y **`charts-screenshot.png`** (1200×630), metadatos y textos en diccionarios (`seo.mixes`, `seo.charts`); lógica en **`src/lib/og-section-images.ts`** (regeneración: `npm run og:sections` según comentario del archivo).
- **Compartir canción con autoplay (`TrackShareButton`):** nuevo botón 🔗 por fila en todos los listados de canciones (`ChartView` — 40 Breaks + New Releases — `TracksSection` propia y pública, y `BeatportTopTracks` de artistas/sellos). Usa `navigator.share` en móvil con fallback a `clipboard` y copia URLs canónicas del tipo `/[lang]/charts?week=<YYYY-MM-DD>&play=<chart|featured>:<uuid>` o `/[lang]/<artists|labels>/<slug>?play=beatport:<beatportId>`. Helpers en **`src/lib/share-track.ts`** (`buildTrackSharePath`, `buildBeatportSharePath`, `parsePlayParam`). `ChartView` y `BeatportTopTracks` detectan el `?play=` al montar, abren el acordeón correcto, hacen scroll a la fila, la destacan y lanzan `playPreviewQueue`. Además `generateMetadata` de `charts/page.tsx`, `artists/[slug]/page.tsx` y `labels/[slug]/page.tsx` sobreescribe OG con la portada + título del track cuando el link lleva `?play`, para que WhatsApp/X muestren la canción concreta. Cuando el navegador bloquea el autoplay (pestaña nueva sin gesto), `DeckAudioProvider` marca `previewBlocked` y pinta un overlay **`PreviewAutoplayOverlay`** con un único botón "▶ TOCA PARA ESCUCHAR" que arranca la reproducción al primer tap. La carátula del track en la tarjeta usa **`next/image`** (ruta `/_next/image`, mismo truco que el resto del sitio), no `<img>` directo, para esquivar el **hotlink block** de `geo-media.beatport.com` (403 con Referer ajeno). Si la imagen no carga (`onError`), se muestra un placeholder `♪` en lugar del icono de imagen rota. Los vinilos mantienen su enlace externo (Discogs/YouTube) por limitaciones del iframe. Detalle en **`docs/USER_ENGAGEMENT.md`** (sección *Track-level deep-linking*).
- **Buscador global (⌘K / Ctrl+K):** `CommandPalette` + API **`src/app/api/search/route.ts`** consultan 9 orígenes (artistas, **tracks en `chart_tracks` / `chart_featured_tracks` / `chart_vinyl_tracks`**, mixes, eventos por lineup, sellos, escenas, posts, organizaciones). Búsqueda sobre columnas denormalizadas **`artist_names_text`** (migración **051**) y **`lineup_text`** (migración **052**, incluye `stages[].lineup`). **Dedupe de tracks** por `title|mix|primer_artista` priorizando `chart_tracks > chart_featured_tracks > chart_vinyl_tracks`, y dentro de cada tabla ordenando por **`chart_editions.week_date` DESC** + `position` ASC para que el resultado sea la fila de la **edición más reciente** (asegura deep-link al DOM). **Eventos pasados** se descartan salvo que todos los demás tipos estén vacíos (búsqueda claramente de evento); chip de fecha **amarillo** futuro / **rojo** pasado. **Carátulas** vía `next/image` (proxy); **mixes** usan foto del artista y fallback **`/images/disco_optimal_breaks.webp`**. Deep-links con `#chart-row-<id>`, `#chart-vinyl-row-<id>`, `#mix-<id>` + `?play=1` → `useEffect` en `ChartView`/`MixesExplorer` abre acordeón, hace scroll y lanza autoplay. Documentado en **`README.md`** (*Global search*) y **`README.es.md`** (*Buscador global*).
